#include<stdio.h>
int main()
{
   printf("Enter the number how many names you want to enter?");
   int x;
   scanf("%d",&x);
   int a[10];
   for(int i=0; i<x; i++)
   {
     printf("Enter names:");
     scanf("a[i]=%d",&a[]);
   
   }
}
