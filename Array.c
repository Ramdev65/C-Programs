#include<stdio.h>
int main()
{
   printf("Enter the value of elements in Array :");
   int n,A[n];
   scanf(%d,&n);
   printf("now enter the values of array");
   for(int i=0; i<n; i++)
   {
      scanf("%d"&A[]);
   }
}
